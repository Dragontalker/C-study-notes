#include <stdio.h>
#include <stdlib.h>

int main()
{
    char str[128];

    printf("Please enter url of FishC: ");
    scanf("%s", str);

    printf("The URL of FishC Studio is: %s\n", str);

    return 0;
}
